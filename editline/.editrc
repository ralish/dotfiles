# Editline configuration
# https://man.netbsd.org/editrc.5
#
# Last reviewed release: v20250104
# Upstream source: https://cvsweb.netbsd.org/bsdweb.cgi/src/lib/libedit/
# Default file paths in preference order:
# - $EDITRC
# - ~/.editrc

# -------------------------------- Settings -----------------------------------

# Enable `editline` functionality
edit on

# Maximum number of history entries
history size 1024

# Only keep unique history entries
history unique 1

# ------------------------------ Key bindings ---------------------------------

# To check the codes being sent from key sequences use `showkey -a`. If a key
# sequence doesn't return any output it's most likely already being handled by
# the terminal driver. You can check its configuration using `stty -a`.

# Default key bindings
#
# Valid options:
# -e    Emacs-style
# -v    vi-style
bind -e

# Jump to the beginning/end of line
bind "\e[1~" ed-move-to-beg                 # Home
bind "\e[4~" ed-move-to-end                 # End

# Insert/delete characters
bind "\e[2~" em-toggle-overwrite            # Insert
bind "\e[3~" ed-delete-next-char            # Delete

# Perform a command history prefix search
bind -k up ed-search-prev-history           # Up
bind -k down ed-search-next-history         # Down

# Move to the adjacent word
bind "\e[1;5D" ed-prev-word                 # Ctrl+Left
bind "\e[1;5C" em-next-word                 # Ctrl+Right

# Incremental backwards history search
bind "^R" em-inc-search-prev                # Ctrl+R

# vim: syntax=conf cc=80 tw=79 ts=4 sw=4 sts=4 et sr
